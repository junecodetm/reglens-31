import { OverviewContent } from "./OverviewContent";

// Route title comes from the root layout metadata (full brand title on /).
export default function OverviewPage() {
  return <OverviewContent />;
}
