# Supply-chain posture: no lifecycle scripts, ever. The only package in the
# lockfile with an install script is puppeteer (transitive, optional, dev-only
# via impeccable); its postinstall downloads a browser binary from an external
# CDN. The detector CI path never needs it, and this file makes the local
# allow-scripts block durable in GitHub Actions too.
ignore-scripts=true
