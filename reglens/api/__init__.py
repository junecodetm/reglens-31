"""The RegLens read API: pydantic response models and the OpenAPI document they generate.

There is no application server here by design — see :mod:`reglens.api.schemas`.
:mod:`reglens.store.export_api` materializes these models as static JSON.
"""
