# Python
.venv/
__pycache__/
*.pyc
.pytest_cache/
.ruff_cache/
.hypothesis/

# Node / web
node_modules/
web/.next/
web/out/

# Secrets & local config — never committed (CLAUDE.md §5)
.env
.env.*

# OS / editor
.DS_Store

# Local logs and scratch
logs/
*.log

# Playwright MCP session artifacts (may contain page snapshots)
.playwright-mcp/

# EXTEND-OGC01: full USLM title zips are cached, never committed
data/cache/

# TypeScript incremental build artifact
web/tsconfig.tsbuildinfo
