# Python
.venv/
__pycache__/
*.pyc
.pytest_cache/
.ruff_cache/
.hypothesis/

# Node / web
node_modules/
web/.next/
web/out/

# Secrets & local config — never committed (CLAUDE.md §5)
.env
.env.*

# OS / editor
.DS_Store

# Local logs and scratch
logs/
*.log

# Playwright MCP session artifacts (may contain page snapshots)
.playwright-mcp/

# Full USLM title zips are cached, never committed
data/cache/

# Wrangler local cache/state
.wrangler/

# TypeScript incremental build artifact
web/tsconfig.tsbuildinfo

# Stray root re-download of the inventory CSV (canonical copy is content-addressed under data/raw/)
/Treasury-AI-Use-Case-Inventory.csv
.dev.vars
